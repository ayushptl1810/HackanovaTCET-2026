from typing import Dict, List


def get_fuzzy_matched_schemes_for_phone(mobile_number: str, language_code: str = "en-IN") -> List[Dict[str, str]]:
    """
    Placeholder for future fuzzy matching implementation by teammate.

    Current behavior: returns empty list and keeps integration contract stable.
    """
    return []
